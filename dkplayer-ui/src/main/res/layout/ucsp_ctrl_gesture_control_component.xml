<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="unics.player.control.BaseControlComponent">

    <LinearLayout
        android:id="@+id/ucsp_ctrl_gestureCtrlContainer"
        android:layout_width="160dp"
        android:layout_height="120dp"
        android:layout_gravity="center"
        android:background="#96000000"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <ImageView
            android:id="@+id/ucsp_ctrl_image"
            android:layout_width="36dp"
            android:layout_height="36dp"
            tools:src="@drawable/dkplayer_ic_action_volume_up" />

        <TextView
            android:id="@+id/ucsp_ctrl_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            tools:text="100" />

        <ProgressBar
            android:id="@+id/ucsp_ctrl_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="100dp"
            android:layout_height="3dp"
            android:max="100"
            android:progressDrawable="@drawable/ucsp_ctrl_seekbar_progress_rounded" />

    </LinearLayout>
</merge>
